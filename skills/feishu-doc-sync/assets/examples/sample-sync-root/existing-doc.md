---
title: Existing Feishu Doc
feishu_doc_token: dox_replace_me
feishu_folder_token: fld_replace_if_needed
feishu_sync_direction: push
---

# Existing Feishu Doc

This file shows a mapped document that already exists in Feishu.

- `push-markdown` will require `--confirm-replace` before overwriting it.
- The same mapping can also be stored in `feishu-index.json` instead of front matter.
